# Repo conventions are injected into agent prompts

Klaus injects the configured repository conventions into Implementer Klaus and Reviewer Klaus prompts instead of relying only on agents to discover the right file. Entry-point docs such as `AGENTS.md`, `CLAUDE.md`, and `README.md` are still useful orientation, but injected conventions are the explicit run contract.
