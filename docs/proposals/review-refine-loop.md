# Review Refine Loop

A future workflow could continue after Reviewer Klaus posts comments: send the pull request back to an implementer-style refinement agent, wait for updates, then review again.

This should remain separate from the current review workflow until the state machine is explicit. The key decision is whether Reviewer Klaus ever approves, or whether the loop only produces comments and leaves final approval to a human.
